<div class="cards-simple-wrapper">
    <div class="row">
        {% for p in serviceCategory.providers %}
        <div class="col-sm-6 col-md-4 col-lg-3">
            {% if p.logos[0].url is defined %}
            <div class="card-simple" data-background-image="{{ asset(p.logos[0].url) }}">
                {% else %}
                <div class="card-simple">
                    {% endif %}
                    <div class="card-simple-background">
                        <div class="card-simple-content">
                            <h2><a href="{{ path("provider_detail", {"slug" : p.slug}) }}">{{ p.name }}</a></h2>
                            <div class="card-simple-actions">
                                <a href="{{ path("provider_detail", {"slug" : p.slug}) }}" class="fa fa-search"></a>
                            </div><!-- /.card-simple-actions -->
                        </div><!-- /.card-simple-content -->
                        <div class="card-simple-price">
                                {{ p.phone }}
                        </div>
                    </div><!-- /.card-simple-background -->
                </div><!-- /.card-simple -->
            </div><!-- /.col-* -->
            {% endfor %}
        </div><!-- /.row -->
    </div><!-- /.cards-simple-wrapper -->